<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="JMangaReader.Views.MangaSelector">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <!-- <RowDefinition /> -->
            </Grid.RowDefinitions>
            <Entry Grid.Row="0" x:Name="SearchInput" Placeholder="поиск" />
            <Button Grid.Row="1" Text="Search" x:Name="SearchButton" BackgroundColor="Aqua" />
            <WebView Grid.Row="0" x:Name="Browser" IsVisible="False" />
            <Image Grid.Row="2" x:Name="MangaImage" Aspect="AspectFill" />
            <ListView Grid.Row="2" x:Name="ListView"/>
            <Label Grid.Row="2" x:Name="EmptyMessage" Text="Поиск не дал результата"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>